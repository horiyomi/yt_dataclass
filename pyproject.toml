[tool.poetry]
name = "yt-dataclass"
version = "0.1.0"
description = ""
authors = ["Damilola Ogungbesan"]
readme = "README.md"
packages = [{include = "yt_dataclass"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
